version: "3.9"

services:
  app:
    build: .
    container_name: lux_invoice_app
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - ./.streamlit:/app/.streamlit:ro
    env_file:
      - src/.env
    restart: unless-stopped
