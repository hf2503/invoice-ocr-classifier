version: "3.9"

services:
  app:
    image: compta_lux_3
    container_name: lux_invoice_app_2
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped

